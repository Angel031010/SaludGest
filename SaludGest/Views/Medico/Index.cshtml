@model IEnumerable<SaludGest.DTOs.Medico.MedicoReadDTO>

@{
    ViewData["Title"] = "Médicos";
    int num = 1;
}

<div class="container mt-4">
    <div class="row">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-6">@ViewData["Title"]</h1>

            <a href="@Url.Action("Create", "Medico")" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> Agregar Paciente
            </a>
        </div>

        @await Html.PartialAsync("_Alerts")

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Nombre)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.ApellidoPaterno)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.ApellidoMaterno)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.EspecialidadNombre)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Email)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.DepartamentoNombre)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Activo)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.FechaCreacion)</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@(num++)</th>
                            <td>@item.Nombre</td>
                            <td>@item.ApellidoPaterno</td>
                            <td>@item.ApellidoMaterno</td>
                            <td>@item.EspecialidadNombre</td>
                            <td>@item.Email</td>
                            <td>@item.DepartamentoNombre</td>
                            <td>
                                <i class="@(item.Activo ? "fas fa-check text-success" : "fas fa-times text-danger")"></i>
                            </td>
                            <td>@item.FechaCreacion.ToString("dd/MM/yyyy")</td>
                            <td>
                                <div class="btn-group">

                                    <a href="@Url.Action("Edit", "Medico", new { id = item.MedicoId })"
                                       class="btn btn-sm btn-secondary m-2">
                                        <i class="fa-solid fa-pencil"></i> Editar
                                    </a>

                                    <a href="javascript:void(0);"
                                       class="btn btn-sm btn-info m-2 btn-detalles"
                                       data-id="@item.MedicoId">
                                        <i class="fa-solid fa-circle-info"></i> Detalles
                                    </a>

                                    <a href="@Url.Action("ConfirmDelete", "Medico", new { id = item.MedicoId })"
                                       class="btn btn-sm btn-danger m-2">
                                        <i class="fa-solid fa-trash"></i> Eliminar
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>

    </div>

    <!-- Modal para mostrar detalles -->
    <div class="modal fade" id="medicoDetailsModal" tabindex="-1" aria-labelledby="medicoDetailsModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <!-- Aquí se cargará el contenido parcial por AJAX -->
            </div>
        </div>
    </div>

</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('.btn-detalles').click(function () {
                var id = $(this).data('id');

                $.ajax({
                    url: '@Url.Action("Details", "Medico")/' + id,
                    type: 'GET',
                    success: function (data) {
                        $('#medicoDetailsModal .modal-content').html(data);
                        var modal = new bootstrap.Modal(document.getElementById('medicoDetailsModal'));
                        modal.show();
                    },
                    error: function () {
                        alert('Error al cargar los detalles del paciente.');
                    }
                });
            });
        });
    </script>
}
