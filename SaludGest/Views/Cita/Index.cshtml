@model IEnumerable<SaludGest.DTOs.Cita.CitaReadDTO>

@{
    ViewData["Title"] = "Cita";
    int num = 1;
}

<div class="container mt-4">
    <div class="row">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-6">@ViewData["Title"]</h1>

            <a href="@Url.Action("Create", "Cita")" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> Agregar Cita
            </a>
        </div>

        @await Html.PartialAsync("_Alerts")

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.FechaHora)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.NombreMedico)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.NombrePaciente)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Estado)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Observaciones)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.Activo)</th>
                    <th scope="col">@Html.DisplayNameFor(m => m.FechaCreacion)</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@(num++)</th>
                            <td>@item.FechaHora.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>@item.NombreMedico</td>
                            <td>@item.NombrePaciente</td>
                            <td>@item.Estado</td>
                            <td>@item.Observaciones</td>

                            <td>
                                <i class="@(item.Activo ? "fas fa-check text-success" : "fas fa-times text-danger")"></i>
                            </td>
                            <td>@item.FechaCreacion.ToString("dd/MM/yyyy")</td>
                            <td>
                                <div class="btn-group">

                                    <a href="@Url.Action("Edit", "Cita", new { id = item.CitaId })"
                                       class="btn btn-sm btn-secondary m-2">
                                        <i class="fa-solid fa-pencil"></i> Editar
                                    </a>

                                    <!-- BOTÓN que carga el partial por AJAX en un modal -->
                                    <a href="javascript:void(0);"
                                       class="btn btn-sm btn-info m-2 btn-detalles"
                                       data-id="@item.CitaId">
                                        <i class="fa-solid fa-circle-info"></i> Detalles
                                    </a>

                                    <a href="@Url.Action("ConfirmDelete", "Cita", new { id = item.CitaId })"
                                       class="btn btn-sm btn-danger m-2">
                                        <i class="fa-solid fa-trash"></i> Eliminar
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>

    </div>

    <!-- Modal para mostrar detalles -->
    <div class="modal fade" id="citaDetailsModal" tabindex="-1" aria-labelledby="citaDetailsModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <!-- Aquí se cargará el contenido parcial por AJAX -->
            </div>
        </div>
    </div>

</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('.btn-detalles').click(function () {
                var id = $(this).data('id');

                $.ajax({
                    url: '@Url.Action("Details", "Cita")/' + id,
                    type: 'GET',
                    success: function (data) {
                        $('#citaDetailsModal .modal-content').html(data);
                        var modal = new bootstrap.Modal(document.getElementById('citaDetailsModal'));
                        modal.show();
                    },
                    error: function () {
                        alert('Error al cargar los detalles de la cita.');
                    }
                });
            });
        });
    </script>
}
